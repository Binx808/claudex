# Preset: Generic Project
name: generic
description: Generic project with basic Claude Code workflow setup

src_dir: src
backend_framework: Generic

architecture_tree: |
  ├── src/               # Source code
  └── tests/             # Test suites

layer_description: |
  - **src/**: Application source code
  - **tests/**: Test suites

layer_rules: {}

lint_dirs:
  - src/
  - tests/

lint_extensions:
  - .py
  - .ts
  - .tsx
  - .js

quick_start: |
  # Install dependencies
  # Start development server

test_command: "pytest tests/ -v"
lint_command: "echo 'Configure linter for your project'"
